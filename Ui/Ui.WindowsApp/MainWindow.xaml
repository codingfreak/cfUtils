<Window x:Class="codingfreaks.cfUtils.Ui.WindowsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:codingfreaks.cfUtils.Ui.WindowsApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" DataContext="{Binding Main, Source={StaticResource Locator}}">
    <DockPanel LastChildFill="True">
        <Button DockPanel.Dock="Top" Content="test" Command="{Binding TestCommand}"></Button>
        <DataGrid DockPanel.Dock="Bottom" ItemsSource="{Binding People.ItemsView}" IsSynchronizedWithCurrentItem="True"/>
    </DockPanel>
</Window>
